const defaultState = {
    inputValue: '',
    searchList: [],
    historyList: []
}
export default (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}